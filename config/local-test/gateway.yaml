# rest -> smtp
rest_listener:
  #use_gunicorn: true
  use_hypercorn: true
  addr: ["[::0]", 8001]
  cert: localhost.crt
  key: localhost.key

  gc_interval: 5
  gc_tx_ttl: 5
  # blobs only need to live from blob upload to PATCH the blob into
  # the tx which should be timely
  gc_blob_ttl: 60

smtp_output:
  ehlo_host: frylock

# smtp -> rest
smtp_listener:
  services:
    - addr: [localhost, 1025]
      endpoint: mx
    - addr: [localhost, 1587]
      endpoint: msa

rest_output:
  - name: mx
    host: inbound-gw
    endpoint: https://localhost:8000/
    verify: localhost.crt
  - name: msa
    host: outbound-gw
    endpoint: https://localhost:8000/
    verify: localhost.crt


